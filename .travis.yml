language: cpp

compiler:
  - clang

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libboost-dev

script:
  - mkdir release
  - cd release
  - cmake -DCMAKE_BUILD_TYPE=Release ..
  - make

after_script:
  - cd release
  - ./kangaru_di_sample